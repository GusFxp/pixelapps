<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Editar App - PixelApps</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Pacifico&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1>Editar App: <span class="title-highlight"><%= app.name %></span></h1>

    <form action="/admin/apps/edit/<%= app.id %>" method="POST">
      <label for="name">Nome</label>
      <input
        type="text"
        id="name"
        name="name"
        value="<%= app.name %>"
        required
      />

      <label for="description">Descrição</label>
      <textarea id="description" name="description" rows="4">
<%= app.description %></textarea
      >

      <label for="version">Versão</label>
      <input
        type="text"
        id="version"
        name="version"
        value="<%= app.version %>"
      />

      <label for="platform">Plataforma</label>
      <input
        type="text"
        id="platform"
        name="platform"
        value="<%= app.platform %>"
      />

      <label for="downloadUrl">URL para Download</label>
      <input
        type="text"
        id="downloadUrl"
        name="downloadUrl"
        value="<%= app.downloadUrl %>"
      />

      <button type="submit">
        Salvar
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M5 12h14M12 5l7 7-7 7"
          />
        </svg>
      </button>
    </form>

    <form
      action="/admin/apps/delete/<%= app.id %>"
      method="POST"
      onsubmit="return confirm('Confirma exclusão do app <%= app.name %>?');"
      style="margin-top: 20px"
    >
      <button
        type="submit"
        style="background-color: #ff4d4d; box-shadow: 0 0 15px #ff4d4dcc"
      >
        Excluir
      </button>
    </form>

    <a href="/admin/apps">Voltar à lista</a>
  </body>
</html>
